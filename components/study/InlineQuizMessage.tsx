'use client';

import React from 'react';

interface InlineQuizMessageProps {
  question?: any;
  onAnswer?: (answer: string) => void;
}

export function InlineQuizMessage({ question, onAnswer }: InlineQuizMessageProps) {
  return null; // Stub component for waitlist
}
